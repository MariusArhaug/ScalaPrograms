// This is pretty much just Java code, but it works in Scala too

/*

val name = "Jesper"
var age = 21
age = 8

if (age > 18) {
    print(name + " can vote")
} else {
    print(name + " can't vote")
}

*/